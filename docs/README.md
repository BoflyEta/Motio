# docs

`demo.gif` belongs here — a screen capture of the demo gallery, referenced from the top of the
root README. Until it is recorded, that image link renders as broken alt text on GitHub.

Suggested capture: the hero splitText, then hover across four or five cards, then flip the
reduce-motion toggle so the difference is visible.
